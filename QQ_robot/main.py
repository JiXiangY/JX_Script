# -*- coding: UTF-8 -*-
import time
import threading
from Configure_Info import configure


def main():
    configure()
    # p = threading.Thread(target = absd, args = (3,))
    # p.start()


if __name__ == "__main__":
    main()